<div class="float-left">
    <a href="profile.html?id={{userId}}"><img class="rounded-circle mr-3" style="width: 40px;" src="{{userImage}}" alt=""></a>
</div>
<div>
    <div><strong>{{userName}}: </strong>{{comment}}</div>
    <div>
        <small>{{date}}</small>
    </div>
</div>
